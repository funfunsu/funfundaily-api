{
  "name": "funfun-task-mcp-server",
  "version": "0.1.0",
  "description": "MCP server exposing funfun open task APIs (getToDoTaskList / getNextTodoTask / checkInTask) over Bearer-token auth.",
  "type": "module",
  "bin": {
    "funfun-task-mcp-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  }
}
